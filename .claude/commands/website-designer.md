Read `website-designer/SKILL.md` in full, then apply the complete visual system to `index.html`.

Transform the existing page into a premium branded product following every component in the skill exactly — typography, animated gradient hero, frosted glass nav, glassmorphism cards, marquee conveyor belt, hover polish, scroll reveal, restraint rules.

Preserve all existing content and functionality. Return a single complete updated `index.html`.

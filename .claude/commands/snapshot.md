Save the current state of index.html as a numbered backup snapshot.

Do the following:
1. Check which backups already exist in `backup/` (prompt1-result.html, prompt2-result.html, etc.)
2. Determine the next snapshot number based on what exists
3. Copy `index.html` to `backup/promptN-result.html` (where N is the next number)
4. Confirm: "Snapshot saved → backup/promptN-result.html"

If index.html doesn't exist, say so and do nothing.
